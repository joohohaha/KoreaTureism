<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.saem.domain.sboard">
	<select id="select_list" resultType="com.saem.domain.SBoardVO">
		SELECT * FROM sboard ORDER BY bid DESC limit #{pageNum}, 5;
	</select>
	
	<select id="select" resultType="com.saem.domain.SBoardVO">
		SELECT * FROM sboard WHERE bid = #{bid} <!-- mybatis 문법 -->
	</select>
	
	<delete id="delete">
		DELETE FROM sboard WHERE bid = #{bid}
	</delete>
	
	<update id="update">
		UPDATE sboard SET btitle = #{btitle}, bcontent = #{bcontent} WHERE bid = #{bid}
	</update>
	
	<insert id="insert">
      INSERT INTO sboard(bname, btitle, bcontent, bdate, bhit) VALUES(#{bname}, #{btitle}, #{bcontent}, now(), 0)
   	</insert>
	
	 <update id="uphit">
           UPDATE sboard SET bhit = bhit + 1 WHERE bid = #{bid}
     </update>
	
</mapper>